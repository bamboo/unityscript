/*
enum Options:

	each = 42

def each(each as Object):
	pass

each = 'Test'

for each in (1, 2, 3):
	print(each)
*/

enum Options {
	each = 42 
}

function each(each: Object) {
}

var each = "Test";
for each (var each in [1, 2, 3])
	print(each);